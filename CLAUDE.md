# CLAUDE.md

## Versioning

When bumping the version in `plugins/branch-compare/plugin.json`, you MUST also update the version in `.claude-plugin/marketplace.json` to match. Both files must always stay in sync.
